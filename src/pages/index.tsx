import { useAuth } from '@/context/AuthContext'; import AuthForm from '@/components/AuthForm'; import { useRouter } from 'next/router'; import { useEffect } from 'react'; export default function Home() { const { user, loading, signOut } = useAuth(); const router = useRouter(); useEffect(() => { if (!loading && user) { router.push('/dashboard'); } }, [user, loading, router]); if (loading) { return <div className="flex items-center justify-center min-h-screen">Cargando...</div>; } return ( <div className="flex flex-col items-center justify-center min-h-screen bg-gray-100"> {!user ? ( <AuthForm /> ) : ( <div className="p-8 bg-white rounded shadow-md text-center"> <h1 className="text-2xl font-bold mb-4">Bienvenido, {user.email}!</h1> <p className="mb-4">Has iniciado sesión correctamente.</p> <button onClick={async () => { await signOut(); router.push('/'); }} className="py-2 px-4 bg-red-600 text-white rounded hover:bg-red-700"> Cerrar Sesión </button> </div> )} </div> ); }